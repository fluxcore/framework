<?php

return array(

	// --------------------------------------------------------------------- //
	// Asset Public URL Override
	// --------------------------------------------------------------------- //
	// When this value is set to anything other than an empty string, it
	// will override the URL that Asset::pub(...) will use to construct
	// full asset URLs.
	// --------------------------------------------------------------------- //

	'public' => '',

	// --------------------------------------------------------------------- //
	// Asset CDNs
	// --------------------------------------------------------------------- //
	// Here you can specify any CDNs that the asset manager should index.
	// These can later be used to construct asset URLs as following:
	// Asset::cdn('identifier', 'path/to/file.css');
	// --------------------------------------------------------------------- //

	'cdn' => array(

		// 'identifier' => 'http://url.com/',

	),

);